{

    "version": "0.2.0",
    "configurations": [
        {
            "name": "Connect to admission-webhook - tilt",
            "type": "go",
            "request": "attach",
            "mode": "remote",
            // "remotePath": "${workspaceFolder}",
            "port": 5555,
            "host": "127.0.0.1"
        },
    ]
}